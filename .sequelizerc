const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'src', 'shared', 'database', 'config,ts'),
  'migrations-path': path.resolve(__dirname, 'src', 'shared', 'database', 'migrations'),
}
